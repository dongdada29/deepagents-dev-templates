# QuickStart

```bash
# Install dependencies
uv sync

# Start the interactive REPL
uv run deepagents-app-py chat

# Run a one-shot prompt
uv run deepagents-app-py ask "Hello, what can you do?"

# Start the ACP server for IDE integration
uv run deepagents-app
```
